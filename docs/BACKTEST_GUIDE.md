# Backtest Guide

1. `python main.py` 실행
2. 상단 탭에서 `백테스트` 선택
3. 전략, 종목, 초기자금, 기간 입력
4. `백테스트 시작` 클릭
5. 결과는 `data/backtests/`에 JSON으로 저장됩니다.
6. CSV 리포트는 `backtest/reports/`에 저장됩니다.

현재 버전은 실거래가 없는 모의 백테스트입니다.
